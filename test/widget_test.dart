import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test - app uses Hive/Firebase which require device initialization
    expect(true, isTrue);
  });
}
